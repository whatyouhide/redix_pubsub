language:
  elixir

matrix:
  include:
    - otp_release: 19.1
      elixir: 1.6.6
    - otp_release: 21.0
      elixir: 1.7.3

services:
  - redis-server

script:
  - if [[ `elixir -v` = *"1.6"* ]]; then mix format --check-formatted; fi
  - mix test --trace
